image:
  resources:
    limits:
      cpu: "1"
      memory: "2Gi"
    requests:
      cpu: "500m"
      memory: "1Gi"

podScheduling:
  nodeAffinity:
    key: "topology.kubernetes.io/zone"
    values:
      - "northamerica-northeast1-a"

spot:
  enabled: false
